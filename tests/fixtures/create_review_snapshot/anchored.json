[
  {
    "path": "src/api/auth.py",
    "line": 42,
    "severity": "important",
    "type": "bug",
    "body": "**Drop `session.token` from the warning log.** It writes the token in plaintext on the warning branch; redact before emit."
  },
  {
    "path": "src/api/auth.py",
    "line": 60,
    "end_line": 75,
    "severity": "nit",
    "type": "refactor",
    "body": "**Split `parse_and_persist` into two functions.**\n\n- Parses, validates, and persists in one call\n- Splitting makes failure modes orthogonal\n- Each function then tests in isolation"
  }
]
