---
description: Review a single PR end-to-end and emit a JSON review payload.
argument-hint: <pr-url> [--agent <name>]
---

TODO: orchestration prompt. Loads `.pr-review.yaml`, dispatches to active review agents, merges into one pending-review JSON.
