---
description: Review a single PR end-to-end and emit a JSON review payload.
argument-hint: <pr-url> [--persona <name>]
---

TODO: orchestration prompt. Loads `.pr-review.yaml`, dispatches to active personas, merges into one pending-review JSON.
